# `.claude/rules/` の規約

このディレクトリには、顧客リポジトリの `.claude/rules/` へ配る規約ファイルを置きます。
**この README は配りません**（`*.md` を一括でコピーしないこと）。

決まり:

- frontmatter の `paths` を必ず書く（ルートからのフルパス。顧客は常にルートで起動する）
- 末尾に `## なぜ` の節を置く。`/shinnn-app:why` がここを引用する
