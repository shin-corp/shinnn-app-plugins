## 対応 Issue

Closes #

<!-- 1 Issue = 1 ブランチ = 1 PR。この行が無いと CI の規約チェックが失敗します。 -->

## 目的

<!-- 何を解決するのかを 1〜3 行で。「なぜ今これをやるのか」が伝わるように書きます。 -->

## 変更点

<!-- パッケージごとに箇条書き。体言止めで書きます。
- shared: 〜の API 定義の追加
- server: 〜エンドポイントの追加
- client: 〜画面の追加
-->

## 対応した受入条件

<!-- docs/仕様書.md の AC-n を列挙し、それぞれを確認したテスト名を書きます。
- AC-1: `AC-1 一覧が新しい順に並ぶ`（server/tests/items.test.ts）
-->

## 確認した結果

<!-- /shinnn-app:check の結果を貼ります。すべて緑であること。
API 定義（shared/src/api/）を変えてテストを変えないときは、この欄の中に「テストを変えない理由: <理由>」の 1 行を書きます（CI が見ます）。
このコメントの中に書くと CI は読みません。コメントの外に書きます。箇条書き・番号付き・太字・引用の形でも通ります。
見出し（## テストを変えない理由）にすると通りません。 -->

- [ ] `npm run check`（型検査 + lint）
- [ ] `npm test`（テスト）
- [ ] 受入条件のカバレッジ
- [ ] API 定義のカバレッジ
- [ ] 受入条件を足した・変えたとき、不具合を直したときは、実装を壊すとそのテストが落ちること（`/shinnn-app:mutation-check`）

## 変更後の状態

<!-- この PR がマージされた後、アプリはどこまでできているか。利用者の言葉で 2〜3 行。
引き継ぐ人がここと「次の一手」だけ読んで続きを始められるように書きます。 -->

## 次の一手

<!-- 次に作るべきもの。無ければ「無し」。 -->

## 規約で迷った点

<!-- 判断に迷った箇所、規約に書いていなかった箇所。無ければ「無し」。
ここに書かれた内容をシン株式会社が月次で読み、規約に反映します。 -->

## 見送った指摘

<!-- レビューで見送った指摘と、その理由。無ければ「無し」。 -->
